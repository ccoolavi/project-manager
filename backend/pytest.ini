[pytest]
testpaths = tests
# tests/ is deliberately not a package: the helpers in conftest.py are imported
# by name from the test modules, which only works when pytest inserts the test
# directory onto sys.path (rootdir-relative "rootdir" import mode).
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
