# Public HTTPS base URL of the FastAPI server.
# GitHub Pages is served over HTTPS, so this MUST be https:// — a plain http://
# endpoint is blocked by the browser as mixed content.
VITE_API_URL=https://your-api-host.example.com
VITE_APP_NAME=KaizenPM
